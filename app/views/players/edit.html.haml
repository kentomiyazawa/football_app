
.main
  .common-form
    %h1.common-form__title
      選手編集
    - if @player.errors.any?
      .error-message
        %ul
          - @player.errors.full_messages.each do |message|
            %li
              = message
    = render partial: "form"
