
.main
  .common-form
    %h1.common-form__title
      新規選手入力
    - if @player.errors.any?
      .error-message
        %ul
          - @player.errors.full_messages.each do |message|
            %li
              = message
    = render partial: "form"

  

    

