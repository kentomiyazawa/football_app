.main
  .blog-form 
    %h1.blog-form__title
      新規投稿
    - if @blog.errors.any?
      .error-message
        %ul
          - @blog.errors.full_messages.each do |message|
            %li
              = message
    = render partial: "form"