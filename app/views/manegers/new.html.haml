.main
  .common-form 
    %h1.common-form__title
      新規監督
    - if @maneger.errors.any?
      .error-message
        %ul
          - @maneger.errors.full_messages.each do |message|
            %li
              = message
    = render partial: "form"